import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ticketsumae25.app',
  appName: 'tickets umae25',
  webDir: 'dist/tickets/browser'
};

export default config;
